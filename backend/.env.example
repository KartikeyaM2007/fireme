# Copy to backend/.env before starting the API.
# AI provider configuration. Groq is OpenAI-compatible and supports transcription and chat.
AI_PROVIDER=groq
DATABASE_URL=postgresql+psycopg://postgres.project-ref:replace-with-url-encoded-password@your-session-pooler-host:5432/postgres?sslmode=require
CLERK_ISSUER=https://your-instance.clerk.accounts.dev
CLERK_JWKS_URL=https://your-instance.clerk.accounts.dev/.well-known/jwks.json
CLERK_AUTHORIZED_PARTIES=http://localhost:3000
GROQ_API_KEY=
GROQ_SUMMARY_MODEL=llama-3.3-70b-versatile
GROQ_TRANSCRIBE_MODEL=whisper-large-v3-turbo
# Alternative provider for transcription, AI summaries, action extraction, and Ask FireMe.
OPENAI_API_KEY=
# Cost/quality controls; change only if your OpenAI account supports another model.
OPENAI_SUMMARY_MODEL=gpt-5-mini
OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe-diarize
# Comma-separated frontend origins in production.
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Disable legacy unowned demo rows in production; signed-in users receive private starter meetings.
SEED_DEMO_DATA=true
# Local storage is used by default. Mount this directory as persistent storage when deployed.
UPLOAD_DIR=./uploads
# Durable uploads: media bytes are also stored in Postgres (media_blobs) so Render restarts keep files.
# Optional Supabase Storage (overrides Postgres blobs when both are set):
# SUPABASE_URL=https://your-ref.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_STORAGE_BUCKET=meeting-uploads

